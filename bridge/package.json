{
  "name": "wavedrop-bridge",
  "version": "1.0.0",
  "description": "Local yt-dlp bridge server for the WaveDrop Chrome extension",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
